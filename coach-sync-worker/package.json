{
  "name": "matmind-coach-sync-worker",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --experimental-strip-types --test src/sharedMatchMediaUpload.test.ts"
  },
  "devDependencies": {
    "wrangler": "^3.114.1"
  }
}
