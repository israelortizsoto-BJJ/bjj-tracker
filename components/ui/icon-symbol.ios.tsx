/**
 * Legacy `IconSymbol` (iOS SF Symbols path) disabled — no external symbol fallback.
 * Use `import Icon from "src/components/Icon"` for SVG icons.
 */

export function IconSymbol(_props: { name?: string; size?: number; color?: string }) {
  return null;
}
