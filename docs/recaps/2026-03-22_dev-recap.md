# BJJ Tracker Dev Recap — 2026-03-22

## Date
2026-03-22

## Branch
`dev` — **ahead of `origin/dev` by 22 commits** (family competition + household + keyboard batch is **committed locally**).  
**Plus** a **dirty working tree**: weekly two-device sync (worker + app wiring) is **present only as uncommitted / untracked changes** at recap time — confirm with `git status -sb` before assuming parity with any remote or another machine.

## Summary
- **Family Competition** on the parent-facing Coach Share weekly surface is now a **real local lane**: add / edit / delete, **format** fields, **month grouping with chevrons**, **multi-kid child chips** (stored selection + roster resolution), and a **stronger family palette** aligned with the weekly story.
- **Coach pilot roster / households**: optional **household label on create**, **roster grouped by household**, and **household editing on existing kid detail** (committed).
- **Coach add-kid form**: **keyboard visibility** regression addressed (committed).
- **Color / story / read-together**: palette pass ties weekly story and competition surfaces more cohesively (committed).
- **Two-device weekly sync loop**: **implemented in the local repo working tree** — invite / link handshake, coach **publish** path (weekly focus → narrow remote document), parent **read** path, **KV-backed worker** (`coach-sync-worker/`) — but **not deployed**, **not committed**, and **not field-validated end-to-end**. **Competition and training data remain local-only** for this milestone; the sync slice targets the **weekly message / focus document** only, not full pilot data replication.

## Process / honesty
- **Dev / local** validation applies to the **committed** family + household work; treat that as “working in development builds on the machine that has these commits,” not as TestFlight or broad tester reality.
- **Weekly sync** needs **worker deployment** (real KV namespace id), **app config / env parity** (`EXPO_PUBLIC_COACH_SYNC_BASE_URL` and `extra.coachSyncBaseUrl` wiring in `app.config.ts`), and **two builds** that actually include the sync code before a meaningful smoke test.
- **Risk:** an **older external tester build** (e.g. on a spouse device) may **not yet include** sync-related app changes — assume **build parity** is unknown until verified.

## Commits (2026-03-22 product batch — committed)
- `83588d7` — Add household editing for existing coach pilot kids  
- `f7873a3` — Fix keyboard visibility in coach kid add form  
- `d5cb4af` — Add family competition child selection for multi-kid households  
- `353f6bd` — Strengthen family palette for weekly story and competition  
- `c1b4369` — Add shared family competition flow and format support  
- `6e10dd7` — Fix family competition add form reset behavior  

## Working tree only (not in the commits above)
- `coach-sync-worker/` (Wrangler worker; `wrangler.toml` still uses KV placeholder id)  
- `src/config/coachSync.ts`, `src/types/coachWeeklySync.ts`, `src/services/coachWeeklySyncApi.ts`, `src/storage/coachWeeklySyncCacheStore.ts`, `src/coach/weeklyFocusPublish.ts`  
- Further edits in `app.config.ts`, Coach Share screens (`index`, `join`, `manage`, `kids`, `kid/[kidId]`), `coachShareStore`, `storageKeys`, `tsconfig.json`, and related types — **confirm with `git diff` / `git status`**

## Gates
- Re-run `npx tsc --noEmit` and `npx eslint .` after the weekly-sync working tree is settled; this recap does **not** assert gates on the uncommitted sync bundle.

## TestFlight / beta reality
- **Build 12** remains the last **documented** coach-testing build in handoff until a new upload is explicitly recorded.  
- **None** of today’s family competition / household / sync work is claimed for **broad TestFlight** here.

## Open loops (tomorrow)
1. **Sync validation first:** deploy or stub-verify worker, set base URL consistently, commit sync code when ready, align **two device builds**, then attempt **coach create invite → parent join/read → coach publish → parent refresh** — still **narrow weekly doc** only.  
2. Continue **internal** QA on family competition + households on a dev build that includes commit `83588d7` (and any follow-up commits).  
3. **Push** `dev` when the tree is clean and gated; remote is **behind** local by 22 commits until then.
