# BJJ Tracker Dev Recap — 2026-03-31

## Date

2026-03-31

## Branch / lane truth

- Branch: **`dev`**; this recap is **Dev lane truth** for end-of-day continuity
- **TestFlight / prod / external tester truth is unchanged** until a new build is uploaded and `docs/dev-handoff.md` is updated—do not treat Dev observations as tester reality

## What was completed

- **Multi-kid / invite alignment:** Parent **This Week** kid selector uses **truly linked kids** for the active invite in Dev; coach roster visibility was improved so invite-linked multi-kid truth is more accurate; parent can open **Athletes on this invite** from the manage-coach link screen; parent and coach views align more clearly on **invite-linked** athletes
- **Household save-state (coach kid detail):** Save control **disabled when not dirty**; **Saved** state after successful save; redundant helper text removed
- **Session persistence Phase 1:** Introduced a raw session persistence boundary—`getSessions()`, `setSessions(next)`, `deleteSessionsForKid(kidId)`—and rewired `app/(tabs)/training/[id].tsx` and `src/storage/coachKidStore.ts`; **no** storage schema, key, or version change
- **Family Huddle / weekly note wording (clarity only):** Coach and parent copy updated so both sides better reflect the **shared-per-invite** published model—**not** an Option B implementation

## Research / model truth (confirmed; not “fixed” in code today)

- Local coach **weekly focus** is **kid-scoped**
- Synced **Family Huddle / weekly note** is **invite-scoped**; **last publish wins** for the whole invite
- **Parent child pills do not switch** which published Family Huddle content is shown—that reflects the **current model**, not a shipped fix today

## QA truth

- **Dev:** Multi-kid invite alignment, household save-state behavior, session persistence Phase 1 wiring, and Family Huddle wording were exercised in the Dev lane as described in handoff notes
- **TestFlight:** No new build or external-tester validation is claimed for this slice

## Known limitations (explicit)

- **One shared Family Huddle / weekly note per invite** remains; parent switching among children does **not** change which published Family Huddle content applies
- **Option B** (per-athlete weekly plans inside one invite) is the **next strategic architecture direction** and is **not implemented**

## Next recommended work block

**Build 21 bridge (Dev):** push cleaner **multi-kid link truth**, clearer **invite athlete management**, and **session persistence write-path** cleanup on top of Phase 1; keep explicit alignment that the **shared-per-invite** Family Huddle limitation persists until Option B is scoped and built.
