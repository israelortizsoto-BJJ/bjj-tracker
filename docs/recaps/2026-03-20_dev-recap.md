# BJJ Tracker Dev Recap — 2026-03-20

## Date
2026-03-20

## Branch
dev

## Summary
- Shipped a **guidance-first** coach **kid profile**: standing **What matters next**, then weekly focus, **How it’s going** check-ins, training, competition.
- **Hardened edit paths** (`entryId` flows, `progress-reflection`, `history` → correct editor).
- **Swipe-to-delete** on kid-detail rows (check-ins, sessions, competitions) backed by **shared storage helpers** (`deleteKidWeeklyFocusEntryById`, `deleteSessionById`).
- **Roster hard-delete** cascade now includes **standing guidance** removal.
- Followed **slice → manual QA → fix** loops; see `docs/dev-handoff.md` for operational detail and QA path.

## Commits
- `56f7b43` — Feat: add coach guidance hero and harden kid pilot editing flows  
- `4532ca7` — Refactor coach kid top stack into guidance-first hierarchy  
- `084b355` — Standardize coach kid row deletion with swipe actions  
- `89f0813` — Add shared delete helpers for coach kid row actions  

## Gates
- `npx tsc --noEmit` passed (with `dev` at `89f0813`)
- `npx eslint .` passed

## Open loops (unchanged priority)
- Ship a **new TestFlight** when ready; Build 12 does **not** include this batch.
- Full internal QA on the expanded kid path (handoff checklist).
- Template/parent Coach Share lane polish and external feedback triage remain **deferred** until this pilot stabilizes.
