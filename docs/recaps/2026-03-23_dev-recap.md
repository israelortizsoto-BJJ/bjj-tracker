# BJJ Tracker Dev Recap — 2026-03-23

## Date
2026-03-23

## Summary
- Recovered MatMind Dev lane and confirmed reliable runtime path is Metro launch into installed Dev Client, not QR relaunch after install.
- Confirmed current sync-dev startup command and environment flow using tunnel networking for stable two-device connectivity.
- Set up and validated two-device Dev workflow with role split: Device 1 Coach, Device 2 Parent.
- Deployed Cloudflare worker and verified invite/redeem path after redeploy.
- Shared-athlete sync slice is now working in Dev: coach invite -> parent accept -> parent adds athlete -> coach sees linked athlete.
- Parent weekly UX got clearer athlete context, clearer training section framing, and a compact practice summary after logging.

## Exact Dev startup rule (current known-good)
Run:

`cd "/Users/ods/Repos/bjj-tracker"`  
`export EXPO_PUBLIC_COACH_SYNC_BASE_URL="https://matmind-coach-sync.ortizdigitalstudio.workers.dev"`  
`npx expo start --dev-client -c --tunnel`

Operational note: after MatMind Dev is installed, open the installed app directly; do not rely on QR relaunch behavior.

## Two-device setup reality
- Device 1 = Coach
- Device 2 = Parent
- Wife's phone required EAS/Apple device registration and inclusion in the development provisioning profile before MatMind Dev could install.

## Product state (Dev truth)
- Role split exists and is active in Dev:
  - Coach role
  - Parent role
  - role picker
  - profile entry behavior differs by selected role
- Worker-backed session currently supports parent redeem + athlete linking support in Dev.
- Shared athlete linking is now verified in Dev two-device smoke.

## Current limitation (not solved yet)
- Parent-entered training logs do not sync back to coach.
- Parent-entered competition data does not sync back to coach.
- Those remain local-only on parent side in this slice.

## TestFlight truth vs Dev truth
- Keep honesty strict: TestFlight build 18 reflects older reality.
- New shared-athlete role-split behavior is currently Dev-validated; do not assume broad TestFlight availability unless explicitly recorded in release notes/handoff.

## Open loops / next likely step
1. Weekly hero / "Read together" presentation still feels somewhat repetitive/confusing and needs another UX pass.
2. Coach visibility for parent training + competition updates is still missing.
3. Next architecture slice should likely be parent-owned competition sync first, then decide training sync model.
