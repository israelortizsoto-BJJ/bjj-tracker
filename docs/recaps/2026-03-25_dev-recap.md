# BJJ Tracker Dev Recap — 2026-03-25

## Date

2026-03-25

## Summary

- Tightened the coach/parent link-state model so parent weekly truth, coach roster truth, and publish readiness are much more consistent
- Fixed the parent auto-relink contamination loop
- Restored a clean fresh-path flow: unlink → fresh invite → intentional reconnect → relink child → publish → parent receive
- Continued polishing Family Huddle / coach-share wording and simplifying the unlink path

## What now works in Dev

- Parent can fully remove link and remain unlinked until intentionally reconnecting
- Coach can create a fresh invite
- Parent can intentionally reconnect with that invite
- Parent can relink an existing child profile
- Coach sees the linked child on roster
- Coach can publish weekly note / Family Huddle
- Parent receives the weekly note / Family Huddle after refresh
- Parent training / competition flow still reflects back on coach in the currently tested Dev loop

## Today’s architecture improvement

Today included a deliberate tightening pass, not only bug-chasing:

- **Canonical invite-token normalization** so invite handling does not drift across surfaces
- **Shared coach-link binding helpers** so coach and parent paths reuse the same binding logic where it matters
- **Stricter parent linked-state selector** for weekly sync — weekly “linked” truth now keys off a redeemed parent channel, not a loose local weekly row
- **Better roster/publish alignment** so what the coach can publish matches what the roster implies
- **Stricter reconnect behavior** instead of a stale “already linked” appearance; fresh invites do not silently re-link the parent

We also used **temporary DEV tracing for auto-relink** to see the bad path clearly; QA only fully passed after the strict linked-state fix landed.

## QA truths from today

Passes:

- Parent stays unlinked after remove-link: **pass**
- Fresh invite does not auto-link parent: **pass**
- Intentional reconnect works: **pass**
- Relink existing child works: **pass**
- Coach publish after reconnect: **pass**
- Parent receives weekly note after reconnect: **pass**
- Fresh-path end-to-end loop: **pass**

## Important product boundaries

- TestFlight still represents older shipped reality until a new build is uploaded — Dev is ahead for link-state and reconnect validation
- Broader training sync is still not the main product target here
- “Add a kid” on the coach side may still need a product decision / cleanup for external testing
- Some wording / polish may still change before the feedback build
- Family Huddle remains family-safe only; coach-only reference content stays private

## Risks / follow-ups

- Need final external-feedback TestFlight readiness pass
- Need explicit must-fix vs acceptable-known-limitation list
- Should review whether any remaining same-device coach/parent edge cases need cleanup before external build
- Still want a final copy / navigation polish pass if time allows

## Next move

Start next session with the final external-feedback TestFlight go/no-go checklist.
